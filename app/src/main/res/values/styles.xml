<resources>

    <style name="TravlogTheme" parent="TravlogBaseTheme" />

    <!-- Base application theme. -->
    <style name="TravlogBaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!--<item name="android:windowNoTitle">true</item>-->
        <!--<item name="android:windowActionBar">false</item>-->
        <!--<item name="android:windowAnimationStyle">@null</item>-->

        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorPrimaryLight">@color/primary_light</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryDark">@color/secondary_dark</item>
        <item name="colorSecondaryLight">@color/secondary_light</item>
        <item name="colorAccent">@color/secondary</item>

        <item name="android:statusBarColor">@color/status_bar</item>
        <item name="android:windowBackground">@color/window_background</item>

        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowAllowEnterTransitionOverlap">true</item>
        <item name="android:windowAllowReturnTransitionOverlap">true</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="TravlogTheme.Splash" parent="TravlogTheme">
        <!--<item name="android:statusBarColor">@color/status_bar</item>-->
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>

    <style name="TravlogTheme.SignIn" parent="TravlogTheme">
        <!--<item name="android:statusBarColor">@color/status_bar</item>-->
    </style>

    <style name="TravlogTheme.MyPage" parent="TravlogTheme">
        <item name="android:statusBarColor">@color/primary_light</item>
    </style>

    <style name="TravlogTheme.SearchLocation" parent="TravlogTheme">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="CustomBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/CustomBottomSheetStyle</item>
    </style>

    <style name="CustomBottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>
</resources>
